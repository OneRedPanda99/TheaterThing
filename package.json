{
  "name": "ghost-light",
  "version": "1.0.0",
  "description": "Live set-piece tracker for a theatre crew: the stage manager calls a scene and every phone follows an animated ground plan.",
  "private": true,
  "scripts": {
    "build": "node build-pages.js",
    "check:pages": "node build-pages.js --check",
    "test": "node test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
